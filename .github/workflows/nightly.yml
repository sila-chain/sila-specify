name: Nightly Update

on:
  schedule:
    - cron: "0 0 * * *" # Runs nightly at midnight UTC
  workflow_dispatch: # Allow manual runs

jobs:
  update-pyspec:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Current Repository
        uses: actions/checkout@v3
        with:
          ref: main

      - name: Install the latest version of uv
        uses: astral-sh/setup-uv@v6

      - name: Clone Consensus Specs
        run: |
          git clone https://github.com/sila-chain/consensus-specs.git

      - name: Apply Patch to Consensus Specs
        run: |
          cd consensus-specs
          git apply ../write_pyspec_dict.patch

      - name: Build Pyspec JSON
        run: |
          cd consensus-specs
          make _pyspec
          cp pyspec.json ../pyspec/nightly/pyspec.json

      - name: Build Links JSON
        run: |
          python3 scripts/generate_links.py consensus-specs pyspec/nightly/links.json

      - name: Commit and Push Changes
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

          git add pyspec/nightly/*.json

          # Check if there are any staged changes
          if ! git diff --cached --quiet; then
            git commit -m "nightly update"
            git push
          else
            echo "No changes detected. Skipping commit."
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
